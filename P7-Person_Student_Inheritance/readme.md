# P7 Person Student Inheritance

## About
This practical shows simple inheritance.

## What it does
A `Student` class gets the `name` property from the `Person` class.

## Code Explanation
- `Student extends Person` means student inherits name.
- We set the name (inherited) and roll number (own).
- `display()` prints both together.
