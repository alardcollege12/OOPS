# P24 Product Price Validation

## About
This practical ensures product prices are not negative.

## What it does
It uses a method to check the price before saving it.

## Code Explanation
- `setPrice()` only works if the price is `0` or more.
- This prevents errors in product data.
- `display()` shows the ID, Name, and Price.
