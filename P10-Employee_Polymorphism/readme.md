# P10 Employee Polymorphism

## About
This practical shows Runtime Polymorphism.

## What it does
One `Employee` variable behaves differently depending on the object it holds.

## Code Explanation
- `e` is an `Employee` variable.
- First, it points to a `Manager` and prints manager bonus.
- Next, it points to a `Clerk` and prints clerk bonus.
