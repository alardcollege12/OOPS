# P25 Person Teacher Inheritance

## About
This practical shows how a Teacher inherits from a Person.

## What it does
A `Teacher` object can use `name` and `age` from the `Person` class.

## Code Explanation
- `Teacher extends Person` is the inheritance relationship.
- We set both inherited and own variables.
- `display()` prints all 4 values.
