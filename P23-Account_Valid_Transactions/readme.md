# P23 Account Valid Transactions

## About
This practical shows secure money transactions.

## What it does
It allows depositing and withdrawing only if the amounts and balance are valid.

## Code Explanation
- `deposit()` checks if amount is positive.
- `withdraw()` checks if there is enough balance.
- `display()` prints the final remaining balance.
