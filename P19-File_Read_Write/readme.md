# P19 File Read Write

## About
This practical writes your input to a file and reads it back.

## What it does
It saves what you type into `file.txt` and then displays it on the screen.

## Code Explanation
- `FileWriter` is used to save the `data` string.
- `BufferedReader` reads the saved content line by line.
- This shows how to both save and open files.
