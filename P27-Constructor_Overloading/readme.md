# P27 Constructor Overloading

## About
This practical shows how to have multiple constructors.

## What it does
It creates a student with default values or with specific name/age.

## Code Explanation
- `Student()` is a no-argument constructor.
- `Student(name, age)` takes parameters.
- Both work differently depending on how we create the object.
