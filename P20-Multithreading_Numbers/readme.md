# P20 Multithreading Numbers

## About
This practical uses two threads at once.

## What it does
One thread prints 1-50 and another prints 51-100 concurrently.

## Code Explanation
- `T1` and `T2` extend `Thread`.
- `start()` runs both loops simultaneously.
- This shows how multithreading works in Java.
