# P12 Shape Method Overriding

## About
This practical overrides the `draw()` method in different shapes.

## What it does
It uses a generic shape reference to draw a circle and a rectangle.

## Code Explanation
- Each shape class has its own `draw()` method.
- When `s.draw()` is called, it prints the name of the actual shape object.
