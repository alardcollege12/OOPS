# P29 Bank Interface

## About
This practical uses an interface for interest rates.

## What it does
It forces banks to have an `interestRate()` method with their own values.

## Code Explanation
- `interface Bank` defines the requirement.
- `SBI` and `HDFC` implement the method.
- We use a `Bank` variable to call methods on different bank objects.
