# P11 Method Overloading

## About
This practical shows how to have two methods with the same name.

## What it does
It can add two numbers or three numbers using the same `add()` name.

## Code Explanation
- One `add()` takes two integers.
- Another `add()` takes three integers.
- Java picks the right one based on the count of numbers given.
