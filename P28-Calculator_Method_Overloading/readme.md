# P28 Calculator Method Overloading

## About
This practical shows complex method overloading.

## What it does
It adds two ints, three ints, or two doubles using the same `add` name.

## Code Explanation
- Three different versions of the `add()` method.
- Java distinguishes them by the number and type of parameters.
