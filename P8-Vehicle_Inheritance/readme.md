# P8 Vehicle Inheritance

## About
This practical shows how different vehicles inherit from one brand category.

## What it does
Both `Car` and `Bike` classes share the `brand` property from the `Vehicle` class.

## Code Explanation
- `Vehicle` has the brand.
- `Car` and `Bike` extend `Vehicle`.
- `show()` method in each class prints the inherited brand.
