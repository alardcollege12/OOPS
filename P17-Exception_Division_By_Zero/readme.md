# P17 Exception Division By Zero

## About
This practical catches math errors.

## What it does
It prevents the program from breaking when we divide by zero.

## Code Explanation
- `10 / 0` is risky code in the `try` block.
- `catch` block catches the error and prints a friendly message.
